<main id="main" aria-role="main">
  <section class="section">
    <div class="container pb-6">
      <%= render partial: 'partials/notifications' %>
      <div class="columns mt-4">
        <div class="column"></div>

        <div class="column is-three-fifths">
          <div class="card">
            <div class="card-content">
              <div class="container has-text-centered pb-4">
                <h1 class="title has-text-dark">Edit Note</h1>
              </div>

              <%= form_for(@note) do |f| %>
                <div class="field">
                  <%= f.label :content, class: 'label' %>
                  <div class="control"><%= f.text_area :content, class: 'textarea', autofocus: true %></div>
                </div>

                <div class="field is-grouped">
                  <div class="control">
                    <%= f.submit "Update note", class: 'button is-link' %>
                  </div>

                  <div class="control"><%= button_to 'Delete note', note_path(@note), method: :delete, class: 'button is-danger' %></div>
                </div>
              <% end %>
            </div>
          </div>
        </div>

        <div class="column"></div>
      </div>
    </div>
  </section>
</main>
